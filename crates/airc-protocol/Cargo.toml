[package]
name = "airc-protocol"
version = "0.1.0"
edition = "2021"
license = "MIT"
publish = false

[dependencies]
airc-core = { path = "../airc-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
ciborium = "0.2"
